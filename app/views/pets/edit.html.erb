<div class="container mt-3">
  <h1>Edit <%= @pet.name %></h1>

  <%= simple_form_for @pet do |f| %>
    <%= f.input :name %>
    <%= f.input :avatar %>
    <%= f.button :submit, class: 'btn btn-primary btn-block' %>
  <% end %>
  
</div>